	<div class="Ticket">
		<ul class="properties">
			<li>Assigned Technicians:</li>
			<ul>
				<% @ticket.technicians.each do |technician| %>
				<li>
					<%= technician.full_name %>
				</li>
				<% end %>
			</ul>
			<li>User: <%= @ticket.user.full_name %></li>
			<li>Asset Tag Number: <%= @ticket.asset.tag_number %></li>
			<li>Title: <%= @ticket.title %></li>
			<li>Description: <%= @ticket.description %></li>
			<li>Status: <%= @ticket.status %></li>
			<li>Deadline: <%= @ticket.deadline %></li>
			<li>
				Priority?: 
				<% if @ticket.priority == true %>
					Yes
				<% else %>
					No
				<% end %>
			</li>
			<li>Updates:</li>
				<ul>
					<li>
						Add a Comment
					</li>
				</ul>
				<ul>
					<% @ticket.comments.each do |comment| %>
					<li>
						<%= comment.comment %>
					</li>
					<% end %>
				</ul>
			<li><%= link_to 'Edit', edit_ticket_path(@ticket) %></li>
		</ul>
	</div>